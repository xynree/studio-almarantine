{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],

  "compilerOptions": {
    "noEmitOnError": false, // allow build even with TS errors
    "skipLibCheck": true, // skip type-checking of libraries
    "strict": false // optional: disable strict type checks
  }
}
